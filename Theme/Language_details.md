#  Language details

* [Что такое defer, порядок выполнения, оптимизации компилятора](#что-такое-defer-порядок-выполнения-оптимизации-компилятора)
* [Как работает init(), порядок вызова в пакете и между пакетами](#как-работает-init-порядок-вызова-в-пакете-и-между-пакетами)
* [Чем отличается new и make](#чем-отличается-new-и-make)
* [Что произойдет если сравнивать слайсы](#что-произойдет-если-сравнивать-слайсы)
* [Что такое alias типов (type-a--b) vs новый тип (type-a-b)](#что-такое-alias-типов-type-a--b-vs-новый-тип-type-a-b)
* [Как работает copy для слайсов](#как-работает-copy-для-слайсов)
* [Что такое rune и byte в Go](#что-такое-rune-и-byte-в-go)
* [Что будет если вернуть из функции ссылку на локальную переменную](#что-будет-если-вернуть-из-функции-ссылку-на-локальную-переменную)

---

## Что такое defer, порядок выполнения, оптимизации компилятора

`defer` откладывает вызов функции до выхода из текущей функции.
Порядок выполнения: **LIFO** (последний добавленный выполняется первым).

Оптимизации: компилятор умеет встраивать `defer` для функций без циклов → быстрее.

Пример:

```go
func f() {
    defer fmt.Println("first")
    defer fmt.Println("second")
}
// Вывод: second \n first
```

---

## Как работает init(), порядок вызова в пакете и между пакетами

* В каждом пакете можно объявить функцию `init()`.
* Вызывается автоматически при импорте пакета, до `main()`.
* В пакете может быть несколько `init()` → выполняются по порядку объявления.
* Между пакетами: порядок инициализации соответствует графу зависимостей (сначала зависимости, потом импортирующие).

---

## Чем отличается new и make

* `new(T)` → выделяет память под тип `T`, возвращает `*T` с zero value.
* `make(T, ...)` → используется только для `slice`, `map`, `chan`, создаёт и инициализирует внутренние структуры.

<pre>
new = просто память
make = готовая к использованию структура
</pre>

---

## Что произойдет если сравнивать слайсы

Сравнение слайсов (`==`) запрещено, кроме сравнения с `nil`.
Для проверки равенства элементов нужно использовать `reflect.DeepEqual` или писать цикл.

---

## Что такое alias типов (type A = B) vs новый тип (type A B)

* `type A = B` → алиас, полностью тот же тип.
* `type A B` → новый тип, с тем же базовым представлением, но не совместим с `B` без явного преобразования.

---

## Как работает copy для слайсов

`copy(dst, src)` копирует `min(len(dst), len(src))` элементов.
Возвращает количество скопированных элементов.

---

## Что такое rune и byte в Go

* `byte` = `uint8`, используется для байтов (ASCII, сырой ввод/вывод).
* `rune` = `int32`, используется для Unicode-кода символа.

---

## Что будет если вернуть из функции ссылку на локальную переменную

Компилятор через escape analysis вынесет переменную в heap, и она будет жить после выхода функции.
Это безопасно, но дороже по памяти (heap + GC).

