# 🟢 Microservices & Integration

* [Как Go используют в микросервисной архитектуре](#как-go-используют-в-микросервисной-архитектуре)
* [Как общаются микросервисы gRPC vs REST](#как-общаются-микросервисы-grpc-vs-rest)
* [Kafka: ключевые вопросы на собеседовании](#kafka-ключевые-вопросы-на-собеседовании)
* [Redis: ключевые вопросы на собеседовании](#redis-ключевые-вопросы-на-собеседовании)
* [Graceful shutdown микросервиса](#graceful-shutdown-микросервиса)
* [Docker](#docker)
* [K8s](#k8s)

---

## Как Go используют в микросервисной архитектуре  


Go часто используют в микросервисной архитектуре, потому что он даёт:  
- Маленький статический бинарь → удобно упаковывать в Docker и деплоить.  
- Высокую производительность и низкое потребление памяти → сервисы выдерживают большую нагрузку.  
- Простую конкурентность → легко обслуживать тысячи запросов параллельно.  
- Богатую стандартную библиотеку (net/http, pprof, testing) без тяжёлых фреймворков.  
- Экосистему вокруг Kubernetes и облаков → Docker, Kubernetes, Prometheus написаны на Go.  
На практике Go используют для написания REST/gRPC API, сервисов-шлюзов, обработки очередей (Kafka, NATS), а также утилит и sidecar-сервисов в Kubernetes.

---

## Как общаются микросервисы gRPC vs REST

- SOAP – устаревший, но стандартизированный протокол с жесткой структурой и встроенной поддержкой корпоративных функций вроде WS-Security для безопасности и WS-Transactions для распределенных транзакций. Он обеспечивает строгую типизацию и автоматическую генерацию клиентов по WSDL-описанию. Минусы: невероятно раздутые XML-сообщения с кучей служебных тегов, что делает его крайне медленным и прожорливым к ресурсам; требует сложной инфраструктуры и практически не пригоден для мобильных приложений из-за высоких накладных расходов.
- REST – гибкий и легковесный подход, использующий простые HTTP-запросы и человекочитаемый JSON, что делает его идеальным для публичных API и веб-сервисов. Он легко кэшируется, масштабируется и интегрируется. Минусы: отсутствие строгих стандартов приводит к проблемам с версионированием API; нет встроенной поддержки сложных операций; слабая типизация может вызывать ошибки; неэффективен для систем реального времени из-за текстового формата.
- gRPC – современный высокопроизводительный протокол, использующий бинарный Protocol Buffers и HTTP/2, что дает минимальные задержки и максимальную пропускную способность, идеально подходит для микросервисов и внутренних систем. Минусы: требует дополнительных инструментов для отладки из-за бинарного формата; менее удобен для веб-браузеров; необходимость поддержки HTTP/2 усложняет развертывание в некоторых окружениях; отсутствие человекочитаемого формата затрудняет ручное тестирование.

---

## Kafka: ключевые вопросы на собеседовании

- Что такое Kafka и зачем нужна?
  - Это распределённая система для передачи сообщений между сервисами. Используется для логов, событий и потоков данных.
- Чем отличается от RabbitMQ?
  - RabbitMQ — классическая очередь, сообщение "исчезает" после обработки. Kafka — это лог, сообщения хранятся заданное время, можно читать заново.
- Что такое topic, partition, offset, broker, consumer group?
  - Topic — “тема” сообщений.
  - Partition — кусок топика для масштабирования.
  - Offset — позиция сообщения внутри партиции.
  - Broker — сервер Kafka.
  - Consumer group — группа потребителей, которые делят сообщения между собой.
- Как работает publish-subscribe?
  - Producer пишет в топик, несколько consumers читают. Внутри группы сообщения делятся, между группами — копируются.
- Что такое retention policy?
  - Время или размер, сколько Kafka хранит сообщения, даже если они прочитаны.
- Как достигается масштабируемость?
  - За счёт партиций — разные потребители могут читать разные партиции параллельно.
- Как обеспечивается надёжность?
  - Репликация партиций между брокерами. Если один падает — данные доступны у других.
- Что такое ISR?
  - In-Sync Replicas — список реплик, которые полностью синхронизированы с лидером.
- Что значит ack=0/1/all?
  - 0 — не ждём подтверждений.
  - 1 — ждём подтверждения от лидера.
  - all — ждём подтверждения от всех реплик.
- At-most-once, at-least-once, exactly-once?
  - At-most-once — может потеряться сообщение.
  - At-least-once — дубли возможны, но не потеряется.
  - Exactly-once — сообщение доставляется ровно один раз.

---

## Redis: ключевые вопросы на собеседовании

Redis — это in-memory база данных, чаще всего используется как кэш, очередь или хранилище быстрых структур данных. Поддерживает разные типы данных, TTL, репликацию и persistence через RDB/AOF.”

- Что такое Redis?
  - In-memory key-value база, очень быстрая.
- Основные типы данных?
  - String, List, Set, Sorted Set, Hash.
- Где применяют Redis?
  - Кэш, pub/sub, очереди, счётчики, хранение сессий.
- Как работает persistence?
  - RDB — периодические снапшоты.
  - AOF — журнал всех команд.
- Что такое TTL?
  - Время жизни ключа, после которого он удаляется автоматически.
- Как масштабируется?
  - Репликация master–replica + шардирование через Redis Cluster.
- Плюсы Redis?
  - Очень быстрый, простая структура.
- Минусы?
  - Всё в памяти → ограничен объёмом RAM.

---

## Graceful shutdown микросервиса

Graceful shutdown — это когда сервис при завершении сначала останавливает приём новых запросов, обрабатывает текущие, закрывает соединения и только потом выключается. В Go это обычно делается через http.Server.Shutdown и обработку системных сигналов, `SIGINT` — когда жмёшь `Ctrl+C` в терминале или `SIGTERM` — когда процессу посылают команду завершиться.

В Go делается так:  

- Используем context.WithTimeout + http.Server.Shutdown(ctx) для HTTP.
- Ловим сигналы через os/signal.Notify.
- Делаем wg.Wait() для горутин, если нужно.

<pre>
Правильный shutdown гарантирует, что сервис не теряет данные и корректно освобождает ресурсы.
</pre>


## Docker
Docker — это платформа для упаковки приложения вместе со всеми зависимостями в контейнер.
  - Контейнер — лёгкая “виртуалка”, которая делит ядро ОС, но изолирована.
  - Позволяет: одинаково запускать сервис на dev/stage/prod, быстро деплоить и масштабировать.
  - Образ (image) = слои, контейнер = запущенный процесс.

<pre>Docker нужен для контейнеризации: упаковываем приложение с зависимостями в контейнер, который одинаково работает в любой среде. Это ускоряет деплой и упрощает масштабирование.</pre>

## K8s

Kubernetes — это система оркестрации контейнеров (обычно Docker).
  - Управляет кластерами: запускает, перезапускает, балансирует, масштабирует контейнеры.
  - Основные сущности: Pod (минимальная единица), Deployment (управление версиями), Service (доступ и балансировка), ConfigMap/Secret (настройки).
  - Поддерживает auto-scaling, self-healing, rolling updates.

<pre>Kubernetes — это оркестратор контейнеров: он автоматически разворачивает, масштабирует и управляет приложениями в кластере. Основные объекты — Pod, Deployment, Service.</pre>
