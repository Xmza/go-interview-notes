# 🟢 Advanced

* [Как устроен reflect и зачем нужен](#как-устроен-reflect-и-зачем-нужен)
* [Что такое generics в Go какие ограничения](#что-такое-generics-в-go-какие-ограничения)
* [Чем отличаются interface{} vs any](#чем-отличаются-interface-vs-any)
* [Что будет если сделать циклическую зависимость пакетов](#что-будет-если-сделать-циклическую-зависимость-пакетов)
* [Как работает unsafePointer и когда его использовать](#как-работает-unsafepointer-и-когда-его-использовать)
* [Что такое atomicValue и когда использовать вместо мьютекса](#что-такое-atomicvalue-и-когда-использовать-вместо-мьютекса)
* [Как устроен syncOnce](#как-устроен-synconce)
* [Зачем нужен build-tags](#зачем-нужен-build-tags)

---

## Как устроен reflect и зачем нужен

`reflect` позволяет работать с типами и значениями во время выполнения. Используется для:

* сериализации (например `encoding/json`);
* ORM;
* написания универсальных библиотек.

Под капотом: `reflect.Value` хранит тип и указатель на данные. Операции медленнее, чем прямой вызов.

---

## Что такое generics в Go какие ограничения

Generics (Go 1.18+) позволяют писать универсальные функции и типы.
Ограничения:

* нельзя специализировать поведение в runtime;
* нет перегрузки методов;
* нельзя сравнивать несравнимые типы.

Пример:

```go
func Sum[T int | float64](a, b T) T {
    return a + b
}
```

---

## Чем отличаются interface{} vs any

* `interface{}` — старый способ обозначить любой тип;
* `any` — алиас для `interface{}` с Go 1.18, используется для читаемости.

---

## Что будет если сделать циклическую зависимость пакетов

Компилятор выдаст ошибку: Go не поддерживает циклические зависимости. Нужно выносить общее в отдельный пакет.

---

## Как работает unsafe.Pointer и когда его использовать

`unsafe.Pointer` позволяет обойти систему типов и работать с памятью напрямую.
Используется редко, например:

* работа с системными вызовами;
* оптимизация сериализации;
* маппинг структур на бинарные данные.

Опасен: можно легко сломать память.

---

## Что такое atomic.Value и когда использовать вместо мьютекса

`atomic.Value` — потокобезопасное хранилище одного значения.

* Подходит для редких записей и частых чтений;
* Быстрее, чем `sync.Mutex` для read-mostly сценариев.

---

## Как устроен sync.Once

`sync.Once` гарантирует, что функция выполнится ровно один раз.
Реализовано через атомик и мьютекс:

* Проверяет флаг `done` атомарно;
* Если `false` — блокирует мьютекс и выполняет функцию;
* Устанавливает `done=true`.

---

## Зачем нужен build-tags

Build tags (`//go:build`) позволяют компилировать код под разные платформы или окружения.
Пример:

```go
//go:build linux
```

<pre>Обычно применяют для кроссплатформенного кода или генерации разных бинарей.</pre>
